<div class="modal-content">
	<div class="modal-header">
        Aggiungi Meriti
    </div>
    <div class="modal-body">
        {{ form_start(form, {'attr': {action:path('character-add-stats', {characterid:characterid})} }) }}
        <div class="row">
        	<div class="col-xs-12">
                <div class="form-group">
                    {{ form_row(form.stat, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.stat) }}
                </div>
        	</div>
        </div>
        <div class="row">
            <div class="col-xs-12">
                <div class="form-group">
                    {{ form_row(form.level, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.level) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12">
                <small>Aggiungi statistiche, il valore consigliato va da 0 a 5, ma per alcuni di essi, ad esempio la BP potresti dover superare il valore <strong>Standard</strong></small>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12">
                <div class="form-group">
                    <button type="submit" class="btn btn-default">Invia</button>
                </div>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
</div>