<div class="modal-content">
    <div class="modal-header">Invia Oggetto</div>
    {{ form_start(form, {'attr': {'action': path('equipment-send', app.request.query.all()) }}) }}
    <div class="modal-body">
        <div class="row">
            <div class="col-xs-12">
                <div class="form-group">
                    {{ form_row(form.receiver, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <div class="form-group">
            <button type="submit" class="btn btn-default btn-lg btn-block">Invia</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>